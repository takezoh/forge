LINEAR_OAUTH_TOKEN=       # OAuth actor=app token (used for both polling and Agent API)
LINEAR_WEBHOOK_SECRET=    # Webhook signature verification secret
# LINEAR_API_KEY=         # Deprecated: fallback when no OAuth token is available
